<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.itsite.demo">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".demo.Activity" />
        <activity android:name=".demo.FragmentActivity" />
        <activity android:name=".demo.RecyclerView" />
        <activity android:name=".demo.WebView" />
        <activity android:name=".demo.AnyView" />
        <activity android:name=".demo.Layout" />
    </application>

</manifest>